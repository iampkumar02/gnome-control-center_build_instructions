# gnome-control-center_build_instructions:
gnome-control-center build requirement inside toolbox for debian (Kali GNU/Linux Rolling)

run 'sh ./shell.sh'
And do the commented steps by your own if required.
